# Prerequisite
Have gradle installed

*Does the user need to set up their javahome?*
# Building

```
# From root of the project :
./gradlew build

# Running the program :
./gradlew run 

# Display more options :
./gradlew run --args="-h"

```